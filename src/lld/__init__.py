"""Local LLM Developer - quality-first local AI software-engineering platform.

Public sub-packages:
    config       - typed settings & workflow loaders
    models       - VRAM-aware model manager & provider adapters
    memory       - filesystem-backed project memory & artifacts
    agents       - the nine specialist agent classes
    orchestrator - sequential execution engine with review loops
    verification - anti-lazy detector + test/lint runners
    api          - FastAPI server (REST + websockets)
    ui           - lightweight HTML/JS dashboard
    cli          - Typer command-line entry point
"""

__version__ = "0.1.0"
